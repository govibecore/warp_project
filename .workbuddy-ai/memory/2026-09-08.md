# 2026-09-08

## Codebase audit (AXIOM)

Deliverable: `C:\axion\CODEBASE_AUDIT.md`

**Key structural discovery:** `C:\axion` (branch `main`) has NO source code — docs only. The real app lives in the git worktree `C:\axion\.worktrees\axiom-mvp` on branch `feat/ui-redesign-desktop`, with ~20 modified / 2 deleted files never committed. Older branches `feat/axiom-mvp` and `feat/ui-redesign` are stale (32 src files vs 36).

**Findings worth remembering:**
- 48.3% of `src/` is dead code (3,806 of 7,880 lines, 35 files) — two orphan roots (`app-shell.tsx`, `efferd-dashboard.tsx`) anchor a kebab-case "efferd" redesign cluster. Live code is the PascalCase generation. Every shadcn primitive except `button`/`select`/`border-beam` is used only by dead files.
- Critical security: hardcoded admin creds (`admin@axiom.com`/`admin`) in `AdminLogin.tsx`; all `convex/admin.ts` queries unauthenticated (exposes student PII + question bank); IDOR in `convex/ai.ts` `getAssessmentData`/`getReport`; `SUPABASE_SECRET_KEY` in root `.env.local` which is NOT gitignored (worktree `.gitignore` covers `.env.local`, root one does not).
- `AdminLayout.tsx` calls `useLocation`/`useNavigate` with no Router provider anywhere → admin route crashes after login.
- Recurring AI-generation defect: garbled concatenated Tailwind classes (`border-borderborder`, `text-foregroundsm`, `bg-/5`) — silent no-ops, 14 sites. Plus `bg-surface`/`text-foreground-secondary` reference tokens not mapped in `@theme inline`.
- `tsc --noEmit` passes with 0 errors but is misleading: `noUnusedLocals` off and NO ESLint/Biome configured at all.
- Product contradiction: `PRODUCT.md` claims offline/localStorage-only/privacy-first, but code uses Clerk + Convex + OpenAI; `SaveStatus` says "Saved to cloud" while `saveSession` only writes localStorage.

## Phase 2: AXIOM transformation (afternoon)

Completed the CTO-led transformation of the AXIOM project. All work in `C:\axion\.worktrees\axiom-mvp`.

**Item bank rewrite (Tasks #10, #12, #13, #14):**
- Replaced single-text-per-item bank with band-differentiated bank: 30 items × 5 developmental bands (Classes 3-4 through 11-12) = 150 variants. Files: `src/data/scenarios/{calibration,energy,ecology,space,data,infra}.ts` + `builder.ts` + `index.ts`.
- Added `misconception?: string` to `AssessmentOption` and `ItemResponse`; `standards?: readonly string[]` to `AssessmentItem` in `src/domain/types.ts`.
- Every designed-wrong (≤2 credit) distractor carries a canonical misconception tag. Full-credit answers never tagged. Verified by probe script `scripts/find-untagged.ts` — zero untagged.
- Every blueprint tagged with NGSS SEP + PISA 2025 competency; `materialize` appends NCF 2023 stage tag.
- Fixed `getDevelopmentalBand(2)` returning `'5-6'` instead of throwing (explicit `>= 5 && <= 6` ranges).
- 14-case data-quality test in `src/data/scenarios/scenarios.test.ts` (item count, ID uniqueness, full-credit uniqueness, misconception tagging policy, competency coverage, determinism, option-order shuffle).

**Report flow (Task #15):**
- Wired `selected.misconception` into `lock()` callback in `Assessment.tsx`.
- `ReportDetail.tsx` response transcript now shows warning icon + misconception text under designed-wrong answers.

**Design system (Tasks #8, #9, #16):**
- Nordic Lagom Design System implemented in `src/styles/global.css` — warm-paper light + Nordic-night dark, OKLCH tokens, single accent (Fjord), hairline elevation, no glass/blur/shadow.
- `CompetencyChart.tsx` rewritten: `--chart-*` tokens, plain tooltip (no blur/shadow), `font-weight: 600`, one accent for student's standing. Added `--chart-1`–`--chart-5` to dark theme.

**Account deletion (Task #17):**
- `deleteAccount` mutation in `convex/users.ts` — cascades to reports + assessments, then deletes user. Leaderboard self-cleans.
- Confirm-dialog button in `StudentDashboard.tsx`.

**Landing copy (Task #18):**
- Fixed STATS from "11 linked scenarios" to "30 assessment items".
- Fixed STEPS body from "Eleven linked STEM scenarios" to "Thirty scenario items across five STEM missions...".

**Documentation (Task #20):**
- `DESIGN.md` — full Nordic Lagom system documentation.
- `TRANSFORMATION_PLAN.md` — per-audit-finding remediation log.
- `PRODUCT.md` — updated to real architecture (Convex + Clerk + OpenAI) with true privacy claims.

**Final state:** 35/35 tests green. `tsc -b` clean. 0 TypeScript errors.
