# Package Manager Rules

- **Use `pnpm` exclusively.** 
- It is explicitly **FORBIDDEN** to use `npm` or `yarn` in this project.
- Always run scripts and installs using `pnpm` (e.g., `pnpm install`, `pnpm dev`, `pnpm test`).
